# Ritual sovereign agent deploy config. Copy to .env and fill in.

# --- chain ---
RPC_URL=https://rpc.ritualfoundation.org
CHAIN_ID=1979

# --- RITUAL to lock into the agent's wallet on deploy. Just a number: 1 = 1 RITUAL (decimals like
# --- 0.5 ok). One wake costs ~0.5-1 RITUAL; 1 is the minimum, 5 gives ~5 wakes of headroom. ---
DEPOSIT=1

# --- harness: 6 = ZeroClaw ---
CLI_TYPE=6

# --- Ritual's own LLM gateway: no external API key needed ---
MODEL=zai-org/GLM-4.7-FP8

# --- first prompt and a unique label per agent ---
PROMPT="Say hello world"
SALT=my-ritual-agent-1